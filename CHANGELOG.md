# 更新日志

## v1.2.1

### 改进

- 「实时状态」的左栏重排成三小节：**时间与日程**（现在几点/什么时段、世界时间对照、下一条日程还有多久）、**正在进行**（当前动作进度条、计划进度、区域 · 地点）、**互动与运行**（最近活跃的人、没回的群聊、刚才在聊什么、本小时剩余额度、发送与模型通道状态）。
- 编辑器加了独立的「模型通道」状态：最近一次主模型调用是成功还是失败、失败原因、多久以前，以及发送失败后的冷却还剩多少秒。
- 动作编辑里的「分组」改成组合框：点一下展开已有分组、输入即过滤，也能直接敲一个新分组名。

## v1.2.0

### 新增

- 新增内置动作「查看日程 / 添加日程 / 删除日程」：她可以自己看、自己加、自己删日程（删只能删她自己加的，用户配的动不了）；同一套能力也注册成了三个工具（`vw_schedule_list` / `vw_schedule_add` / `vw_schedule_remove`），主人格和别的插件也能用。
- 动作新增「指令触发」类型：填一条别的插件的指令 + 参数说明，她会把意图拼成指令交给对方执行，再把返回内容用自己的话讲出来。用户 @ 触发时直接复用那条消息事件（图片、引用都跟着走），自主触发时借用这个会话最近一条事件、不带图片。
- 新增预设：一整套地图 / 动作 / 日程 / 会话白名单可以存成预设文件，随意切换、重命名、删除、直接编辑 JSON（复制走就是导出，贴进来就是导入）。切换预设会先自动备份当前配置，并清空所有会话的位置、数值、计划与群聊留档；**记忆和日志不动**。
- 调试输出新增「改日程」「触发指令」两类事件，日志页同样区分。

### 改进

- 群聊上下文改成「回应过就不再回放」：她答过的内容不再出现在「群里最近在聊」里，改由她顺手写的一句「刚才在聊什么」当背景，避免重复回应老话题；她自己说过的话仍然保留，方便要求她换句式。
- 编辑器「实时状态」的上下文统计拆成「未回应 / 已回应」两个数字。
- 提示词里明确要求：这次行动如果指向具体的人，必须把对方的 id 写进意图，系统才认得。
- 动作库：内置动作排在最前，其余按加入时间倒序；卡片描述最多显示三行，长说明不再把版面撑乱。
- 睡觉时群里刷屏不再逐条写日志：消息照样挡下、照样记进上下文，但日志每 10 分钟最多一条，并会带上这期间一共挡下了多少条。
- 实时状态的名片一栏新增「重新获取」：从平台重新读一次她当前的群名片；之前没记过原名时会把它同时记为原名。

## v1.1.0

### 新增

- 新增内置动作「回想」：她会主动翻自己的记忆，可以按主题、按地点、按区域找（例如「想想在厨房做饭的事」「回忆一下公园里的事」）。回想完不需要再写别的动作，插件会带着回忆到的内容再问她一次，由她用自己的话讲出来；什么都想不起来时会照实说。
- 调试输出新增「回想开始 / 回想完成 / 图片内容」三类，日志页也能看到：想回忆什么、翻到了什么、图片看没看成。
- 没配图片转述模型时，图片会直接交给多模态主模型（自上次回复以来最多若干张，数量可配置）；配了转述模型时先转述成文字再入上下文，成功和失败都会记一条日志。
- 新增「被动回复后的主动发言冷却」：她刚回过话之后的一段时间里，不会再因为孤独感主动搭话（默认 10 分钟，可配置）。
- 动作编辑里新增「固定参数」：填了之后这个参数每次都用你写的值，不再交给模型猜。

### 改进

- 工具参数补全的触发条件放宽：只要工具声明了参数而这次没给全，就会让辅助模型补一次；调用时报“缺少参数”类错误时会带着报错再补一次并重试一次，覆盖“工具 schema 与实现不一致”的第三方工具。
- 提示词层次按“稳定性递减”重排：状态、位置、当前动作、时间在前，记忆、群聊、最近发生的事、其他插件上下文在后；可达表从场景层挪到运行时层。相邻两次调用（例如动作完成后的二次调用）能命中更长的前缀缓存。
- 所有大模型调用点统一显式传入 reasoning 开关，避免同一段提示词出现两种变体导致缓存失效。
- 提示词里会标出「只有在这个地点才能做」的动作，并引导她在当地顺手挑一件做，减少“到一个地方却什么都不做”。
- 记忆召回惩罚改为临时：刚被想起过的记忆只在一段时间内轻一点，出了窗口自动恢复；不再按累计次数永久降权。
- 内置动作（说话 / 移动 / 睡觉 / 想事情 / 分享 / 回想）不能被删除，只能停用；误删或从 JSON 里删掉时会在保存时自动补回并提示。动作库的分类筛选新增「内置」。
- 编辑器调试页会显示每个工具的原始必填列表，方便判断“为什么没给参数”。

### 修复

- 修复多段回复被合并成一条消息的问题（说话内容和动作文案现在会分成多条发出）。
- 修复被排进计划的工具动作丢失意图、到点执行时报“没有给出想做什么”的问题。
- 修复第三方工具把参数标成可选、实现却必填时直接报错的问题。
