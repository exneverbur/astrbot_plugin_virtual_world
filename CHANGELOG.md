# 更新日志

## v1.4.3

### 优化

- 智能搜索的行为优化 并强化了搜索后收敛
- 优化了提示词 加强对 这句话是否是对bot说的 的识别

### 修复

- 修复了内置动作复制后的新动作也被视为内置动作, 导致无法删除的问题

## v1.4.2

### 新增

- 图片转述拆成两段：先看图出描述，再结合当前对话补一句与话题的关系；多张图合并成一次调用
- 图片转述的两个提示词可以在插件配置里改，都带「恢复默认」
- 一个动作的工具可以配成「按顺序都调 / 依次尝试 / 智能选择」三种用法
- 上网搜索优化为智能搜索, 强化搜索功能, 工具型动作新增「联网检索」形态, 搜索更智能(推荐配合使用anysearch插件 或其他带有可以直接根据url获取网页内容的工具的插件)
- 工具连续两次调用失败会临时退避，状态页能看到被停用的工具并手动重试
- 内置动作的「说明」加「恢复默认」
- 天气：查到的结果会当背景写进提示词（标明「几小时前 / 昨天 / X 天前查的」），
  后台按间隔静默刷新，地图页顶部显示当前天气
- 模型配置合并成「看图模型」（多模态）+「打杂模型」（文本）：补工具参数、压缩上下文、
  图片关系分析、天气归一化都走打杂模型

### 修复

- 联网搜索不再出现「缺少必填参数 query」：没写查询词时按她的意图自动补一条
- 「上网搜索」「查天气」不再带默认工具，没选工具时写明原因并跳过
- 检索来源写进日志与调试输出（默认不发到群里），打开开关才允许她讲结果时带一条链接
- 修复偶发的插件未接管, 导致事件漏给主人格直接回复的问题
- 同一条会话里连发的消息改为排队接管：第二条能看见第一条刚说过什么
- 调试回显按实际发生顺序发出：先调工具就先发工具那一行，结果回来再说话
- 修复了规则触发的动作没有意图的问题

## v1.4.1

### 新增

- 消息里的@bot的记录会转换成@你
- 群名片文案改写在动作和地点自己身上，老配置的全局映射会自动搬过去；全局设置里不再配置
- 全局设置新增「管理员 QQ」：这些号也能用重载、重置、改名片、跑日程、调试、会话白名单这些管理指令
- 区域支持批量使用大模型给下面的每个地点生成动作
- 日程动作链的动作选择优化
- 戳一戳失败会写明原因（协议端不支持 / 冷却中等），并自动改走协议端的 `send_poke`
- 新增调试输出类型：戳一戳、心情缓过来了
- 图片转述按图片指纹缓存（持久、重启后仍有效），表情包第二次出现不再调多模态模型

### 修复

- 修复了提示词的最近群聊内容无法看到其他人聊天记录的问题, 优化了群聊上下文的结构

## v1.4.0

### 新增

- 重做情绪系统:
- 情绪拆成两轴：**心潮**（有多激动）× **效价**（心情好坏），心情词与「这一轮的表达方式」都由两轴派生；群聊最多 2 条、私聊 3 条
- 效价由「基线 + 短期偏移」组成：基线跟五维与时段走，事件只产生偏移并随时间回落
- 状态页新增**数值曲线**（心潮 / 效价，可切 6 小时 / 24 小时 / 3 天）与起伏、峰值、低谷三个指标
- 插话分两条动机（想被注意到 / 想发作），阈值随心情浮动；状态页显示插话被哪道闸拦住
- 新增内置的`mood_reset`（长期低落时自我调节）与`storm`（正在气头上）事件
- 调试输出新增「心情缓过来了」类型；编辑器新增效价滑杆与表达方式开关

### 修复

- 正在做的事被顶掉时留下痕迹（日志 + 心情反应），不再无声替换
- 工具调用失败、发送失败、被冷落都会影响心情
- 记忆强度改由情绪两轴数值计算，不再依赖会失效的心情词表

### 未来计划

- 记忆系统重做, 睡眠时可以整理记忆, 模仿真实人类机制
- 场景动态天气系统
- 随机事件系统 仿跑团做事件判定

## v1.3.5

### 修复

- 模型输出不完整的思考标签时，不再把整段思考发到群里
- 同一句话不再在提示词里重复出现（她不会再回一句「你怎么说了三遍」）
- 日程没开「自动先走过去」时，不再偷偷替她走去别的房间

### 新增

- 「上网搜索」「查天气」改为内置动作：不知道的事先查再说，默认工具没装就自动换成本机装好的那个
- 调试输出支持精简模式与分类型勾选（工具的调用与返回分开）
- 调试消息按实际发生顺序发送（先调工具、再说话）
- 编辑器状态页与动作页在工具缺失时给出提示

## v1.3.4

### 修复

- 优化了智能日程的功能和提示词
- 修复了持续动作的指令型动作未执行的问题

## v1.3.3

### 修复

- 「单轮」动作生成的话发不出去的问题
- 日程动作链的工具 / 指令型步骤可以写「意图」了；留空时保存会用内容生成模型补一句、运行时有动作说明兜底，不再直接跳过。

### 新增

- 日程新增智能日程功能, 可以由大模型按配置的动作列表生成执行计划

## v1.3.2

### 修复

- 日程 / 计划里的「单轮」动作没有现成台词时，改用大模型按动作语义现写一句（没模型或额度用完才退回动作文案）。
- 接管回复时补发 AstrBot 的生命周期钩子：等模型（`on_waiting_llm_request`）、用工具 / 用完工具、发完消息（`after_message_sent`）——靠这些钩子贴表情的插件在接管模式下也能正常开始与结束。

### 新增

- 调试输出新增「日程开始执行」一类；日程触发与手动执行都会写进日志（含时间、要跑的动作、是不是手动跑的）。

## v1.3.1

### 修复

- 调试输出不再重放历史日志（重载后会每个 tick 刷同一句老消息）。
- 还没有消息事件时工具动作直接跳过并写明原因，不再报 `'NoneType' object has no attribute ...`。
- 指令 / 工具返回的内容按组件还原成人话，不再把 `MessageEventResult(chain=[...])` 丢给模型。
- 指令 / 工具返回的图片：配了转述模型就先转述，没配就交给多模态主模型。
- 返回内容进提示词前截断到 800 字，base64 图片不再撑爆上下文。
- 指令型动作也遵守「完成后」与全局「工具结果回话」，关掉就不说话。

### 新增

- 「全局设置 → 图片转述」可自定义转述提示词；默认提示词会识别表情包 / 梗图。
- 日程可以「立即执行」：编辑器日程列表 / 属性表单一键跑一遍动作链（忽略时间与条件，不占当天触发名额），群聊里用 `/vw schedule run <id>`。
- 每条日程可以开「智能日程」：到点让大模型给工具 / 指令型步骤补一句「想干什么」，动作链本身原样不动（默认关，开了就不用手写每步的意图）。

## v1.3.0

### 修复

- 日程改完触发时间当天即可生效（去重加上时间点）。
- 日程不再因 tick 漂移整分钟漏触发；被条件挡下会写日志说明原因。
- 插件重载后偶发 `Cannot operate on a closed database`。
- 群里发的图片不再毫无记录，并支持引用消息里的图片。
- 接管回复的钩子改用真实响应的浅拷贝，别的插件不会再因缺字段而收不到。

### 新增

- 打字停顿：分段回复之间按字数停顿，可配可关。
- 内置动作「戳一戳」。
- 说话密度提醒：说得太密时让她这轮少说多做。
- 群聊按一条时间线呈现，并区分「对她说」与「群里插话」。
- 记忆时间标签带上时段（如「09-11 晚上」）。

## v1.2.1

- 实时状态左栏拆成「时间与日程 / 正在进行 / 互动与运行」三小节。
- 新增「模型通道」状态：最近一次模型调用结果、发送失败冷却剩余时间。
- 动作「分组」改成组合框：点开即选，也可直接输入新分组。

## v1.2.0

### 新增

- 内置动作「查看 / 添加 / 删除日程」，并注册为工具 `vw_schedule_list/add/remove`。
- 动作新增「指令触发」类型：把意图拼成别的插件的指令并取回结果。
- 新增预设：整套配置可保存 / 切换 / 重命名 / 删除 / 直接编辑 JSON。
- 调试输出新增「改日程」「触发指令」。

### 改进

- 群聊上下文改为「回应过就不再回放」，改用「刚才在聊什么」当背景。
- 实时状态拆出「未回应 / 已回应」两个数字。
- 提示词要求：动作指向具体的人时必须带上对方 id。
- 动作库：内置排最前，其余按加入时间倒序；描述最多三行。
- 睡觉时群里刷屏的日志节流为每 10 分钟一条。
- 群名片一栏新增「重新获取」。

## v1.1.0

### 新增

- 内置动作「回想」：按主题 / 地点 / 区域回忆，回想完带着结果再说一次。
- 调试输出新增「回想开始 / 回想完成 / 图片内容」。
- 图片转述：配了转述模型就先转述，没配就直接交给多模态主模型。
- 「被动回复后的主动发言冷却」（默认 10 分钟，可配）。
- 动作「固定参数」：填了就不再让模型猜。

### 改进

- 工具参数补全放宽：少参数就补一次，参数类报错再补一次并重试。
- 提示词按稳定性重排，相邻调用能命中更长的前缀缓存。
- 模型调用统一传 reasoning 开关，避免同一段提示词出现两种变体。
- 场景限定动作会标注，引导她到当地顺手做点什么。
- 记忆召回惩罚改为临时，不再按次数永久降权。
- 内置动作只能停用、不能删除，误删会自动补回；分类筛选新增「内置」。
- 调试页显示每个工具的原始必填列表。

### 修复

- 多段回复被合并成一条消息。
- 计划中的工具动作丢失意图。
- 第三方工具参数标成可选、实现必填时报错。
